/**
 * Keyboard
 * Simple input state holder for keyboard and mobile controls.
 * Properties like `LEFT`, `RIGHT`, `SPACE`, `THROW` are set by event handlers.
 */
class Keyboard {
    LEFT = false;
    RIGHT = false;
    UP = false     
    DOWN = false;
    SPACE = false;
    THROW = false;


}
